# Secrets Handling Policy

Status: scaffolded

Secrets, credentials, private URLs, production secrets, endpoints, private logs, and sensitive topology must stay out of public-safe artifacts.

Use placeholders such as `<redacted-secret>`, `<private-url>`, and `<held-endpoint>` only when a template needs to show a field shape.
